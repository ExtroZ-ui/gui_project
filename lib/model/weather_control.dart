import 'package:flutter/material.dart';

// ignore: non_constant_identifier_names
dynamic WeatherControl() {
  return const NavigationRailDestination(
      icon: Icon(Icons.water),
      label: Text('Weather'),
      selectedIcon: Icon(Icons.water));
}
