import 'package:flutter/material.dart';

dynamic viewpointControl() {
  return const NavigationRailDestination(
      icon: Icon(Icons.search),
      label: Text('View'),
      selectedIcon: Icon(Icons.search));
}
